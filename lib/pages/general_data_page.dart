import 'package:flutter/material.dart';

class GeneralDataPage extends StatelessWidget {
  const GeneralDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('General Data Page'),
      ),
    );
  }
}
